# Project Domain — WU-002

Initial domain implementation for `Project`.

## Implemented

- `ProjectId`
- `Project`
- `ProjectStatus`
- core lifecycle invariants
- domain tests

## Scope

This Work Unit does not implement persistence, application use cases, Work Units, dependencies, or runtime integration.

Those remain separate Work Units in the Implementation Plan.
