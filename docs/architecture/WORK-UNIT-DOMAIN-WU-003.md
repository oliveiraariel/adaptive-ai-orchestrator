# Work Unit Domain — WU-003

Initial domain implementation for `WorkUnit`.

## Implemented

- `WorkUnitId`
- `WorkUnit`
- `WorkUnitState`
- state-transition invariants
- domain tests

## Scope

This Work Unit does not implement planning, dependency resolution,
resource selection, persistence, delegation, evaluation, or replanning.

Those remain separate Work Units in the Implementation Plan.
