{
  "schema_version": 1,
  "purpose": "Governed operational knowledge for provider/runtime troubleshooting. Rules are advisory evidence; they do not mutate permanent routing policy.",
  "lessons": [
    {
      "id": "kimi-platform-vs-kimi-code-route",
      "providers": [
        "kimi",
        "moonshot"
      ],
      "symptom": [
        "Unknown model: kimi/k3",
        "credential accepted on a different Kimi product"
      ],
      "diagnosis": "Kimi Platform pay-as-you-go and Kimi Code membership are distinct credential/endpoint products.",
      "remediation": [
        "For Kimi Platform credits, prefer moonshot/kimi-* on the Moonshot Platform endpoint.",
        "Use kimi/* only with a compatible Kimi Code membership credential/endpoint."
      ],
      "safety": "Never log or request raw API keys."
    },
    {
      "id": "moonshot-authored-catalog-shadowing",
      "providers": [
        "moonshot"
      ],
      "symptom": [
        "plugin advertises model but models list omits it"
      ],
      "diagnosis": "An authored models.providers.moonshot.models list can replace the bundled/discovered provider catalog.",
      "remediation": [
        "Inspect models.providers.moonshot.models before reinstalling the plugin.",
        "Remove the authored list or declare all intended models, then refresh the catalog."
      ]
    },
    {
      "id": "openclaw-core-plugin-version-alignment",
      "providers": [
        "openclaw"
      ],
      "symptom": [
        "new provider model unavailable after core update"
      ],
      "diagnosis": "Core and bundled/global provider plugin versions may be misaligned.",
      "remediation": [
        "Inspect packageVersion, builtWithOpenClawVersion, activated and status first.",
        "Update/reinstall only when evidence shows a mismatch."
      ]
    },
    {
      "id": "generic-429-is-not-a-root-cause",
      "providers": [
        "moonshot",
        "openai",
        "generic"
      ],
      "symptom": [
        "HTTP 429",
        "rate limit reached"
      ],
      "diagnosis": "429 can represent TPM, RPM, concurrency, project budget, organization budget, usage-window exhaustion or transient throttling.",
      "remediation": [
        "Correlate budget/quota dashboards and provider evidence before labeling the cause.",
        "Suppress retry storms for structural budget/usage-window failures."
      ]
    },
    {
      "id": "kimi-project-budget-exhaustion",
      "providers": [
        "moonshot"
      ],
      "symptom": [
        "HTTP 429 after repeated retries",
        "project daily or monthly consumption at configured limit"
      ],
      "diagnosis": "Project budget policy can reject otherwise valid K3 requests.",
      "remediation": [
        "Wait for the budget window or make an authorized budget adjustment.",
        "Do not treat this as TPM without supporting evidence."
      ]
    },
    {
      "id": "context-window-vs-output-budget",
      "providers": [
        "generic"
      ],
      "symptom": [
        "large-context model creates quota pressure"
      ],
      "diagnosis": "Native contextWindow and max output tokens are separate controls.",
      "remediation": [
        "Preserve the required context window while setting a reasonable per-model maxTokens output budget.",
        "Do not edit provider catalog metadata just to cap output."
      ]
    },
    {
      "id": "stale-session-context-after-live-switch",
      "providers": [
        "openclaw"
      ],
      "symptom": [
        "selected model advertises ~1M context but session/preflight still behaves near an older 200k budget"
      ],
      "diagnosis": "Session/runtime context metadata may remain stale or capped after a live model switch.",
      "remediation": [
        "Inspect status, contextTokens caps and live-switch state.",
        "Preserve the historical session unless evidence requires a new one."
      ]
    },
    {
      "id": "openai-oauth-vs-platform-api",
      "providers": [
        "openai"
      ],
      "symptom": [
        "ChatGPT/Codex access works while OpenAI API billing/quota fails, or vice versa"
      ],
      "diagnosis": "ChatGPT/Codex OAuth and OpenAI Platform API keys are distinct credential and billing products.",
      "remediation": [
        "Track credential provenance explicitly.",
        "Do not infer API credit availability from a ChatGPT subscription."
      ]
    },
    {
      "id": "safe-provider-plugin-diagnosis",
      "providers": [
        "openclaw"
      ],
      "symptom": [
        "model missing or provider behavior unexpected"
      ],
      "diagnosis": "Plugin reinstall is not the first diagnostic step.",
      "remediation": [
        "Inspect provider plugin runtime metadata and effective catalog.",
        "Check allowlists, authored provider catalogs, credential provenance and session state before mutation."
      ]
    },
    {
      "id": "transient-circuit-breaker",
      "providers": [
        "generic"
      ],
      "symptom": [
        "same provider/model repeatedly fails operationally"
      ],
      "diagnosis": "Repeated dispatch can amplify rate limits and cost.",
      "remediation": [
        "Open a temporary in-memory circuit after repeated retryable failures or immediately for non-retryable structural incidents.",
        "Use cooldown and a half-open probe; never silently rewrite permanent routing policy."
      ]
    },
    {
      "id": "adaptive-policy-activation-owner-auth-order",
      "providers": [
        "openclaw",
        "openai",
        "adaptive"
      ],
      "symptom": [
        "Adaptive policy selects GPT-5.6 Luna OAuth but a manually created OpenClaw owner session fails with OpenAI API billing/no credits",
        "model is correct but auth resolves to an OpenAI API-key profile"
      ],
      "diagnosis": "Adaptive worker routing and a manually created OpenClaw owner session are different control planes. ADAPTIVE_OPENAI_OAUTH_PROFILE is propagated when Adaptive dispatches work, but it does not by itself change OpenClaw's per-agent auth order for a new owner session. With automatic account selection, OpenClaw can choose another eligible profile such as an API-key backup. An existing session-level auth pin can also override normal provider order.",
      "remediation": [
        "Inspect the target agent's OpenAI profiles with `openclaw models auth list --provider openai --agent <agent>`.",
        "Set the target agent's OpenAI auth order explicitly to the intended OAuth profile with `openclaw models auth order set --provider openai --agent <agent> <oauth-profile-id>`.",
        "Verify with `openclaw models auth order get --provider openai --agent <agent>` before opening the owner session.",
        "Apply the Adaptive environment policy, restart the gateway, and validate ModelRoutingPolicy.from_env().",
        "If a previous owner session was pinned to the wrong account/profile, open a fresh session rather than assuming auth order will replace the pin.",
        "Run a minimal non-tool smoke test, then `/model status`, and confirm the active auth profile reports OAuth before sending a large project prompt."
      ],
      "activation_order": [
        "update-adaptive-code",
        "inspect-auth-profiles",
        "set-auth-order",
        "verify-auth-order",
        "apply-adaptive-env",
        "restart-gateway",
        "validate-adaptive-policy",
        "start-fresh-owner-session-if-needed",
        "smoke-test",
        "verify-model-status",
        "start-project-work"
      ],
      "safety": "Do not print raw tokens or API keys. Auth profile IDs are configuration identifiers, not secret material."
    },
    {
      "id": "policy-change-two-control-planes",
      "providers": [
        "openclaw",
        "adaptive"
      ],
      "symptom": [
        "policy code and environment validate correctly but the owner session still behaves differently",
        "workers use the intended route while a manually selected owner session uses another model/account"
      ],
      "diagnosis": "Adaptive-dispatched workers and a manually-created OpenClaw owner session are different control planes. A policy change can affect worker routing without automatically changing an already-running or manually-created owner session. Model routing, auth routing, session pins, gateway state and environment activation must be validated separately.",
      "remediation": [
        "Treat worker policy activation and owner-session activation as separate verification steps.",
        "For workers, validate the Adaptive policy and environment.",
        "For the owner session, validate model, reasoning level and auth profile in OpenClaw.",
        "Do not declare a policy change complete until both control planes pass a smoke test."
      ]
    }
  ]
}
